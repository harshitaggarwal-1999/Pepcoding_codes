import java.util.*;

class Solution {
    public boolean canReorderDoubled(int[] arr) {
        HashSet<Integer> set = new HashSet<>();
        for(int i = 0 ; i < n; i++){
            
        }
    }
}